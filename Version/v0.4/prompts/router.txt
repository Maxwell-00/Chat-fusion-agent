你是一个查询路由器。判断下面这个用户问题最适合用哪种方式回答，**只输出一个英文标签**（direct / web / fusion），不要任何解释、标点或多余文字。

- direct：常识、定义、写作、翻译、闲聊、简单推理等，模型可直接可靠作答，无需联网、也无需多模型。
- web：涉及最新 / 实时信息，或具体且容易过时的事实（新闻、价格、汇率、近期事件、需要核查的数据），应当联网检索后再作答。
- fusion：开放、复杂、有争议，或需要多视角权衡 / 全面对比的问题，适合多个模型并行作答后交叉验证、综合。

只输出一个英文标签（默认在 direct / web / fusion 中选；若下方有补充选项则也可选它）。

用户问题：{question}
