openai>=1.0
python-dotenv>=1.0
httpx>=0.27

# Web UI（app.py）；CLI（main.py）不需要。需 6.x：消息字典格式为默认。
gradio>=6

# 可观测性（可选，仅当设了 FUSION_TRACING 才启用；见 tracing.py）。
# 取消注释安装；查看 UI 另装 arize-phoenix 并 `python -m phoenix.server.main serve`。
# opentelemetry-sdk
# opentelemetry-exporter-otlp-proto-http
# openinference-instrumentation-openai
